import { ExtractedFieldPipe } from './extracted-field.pipe';

describe('ExtractedFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractedFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
