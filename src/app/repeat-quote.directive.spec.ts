import { RepeatQuoteDirective } from './repeat-quote.directive';

describe('RepeatQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
