import { SortRidesPipe } from './sort-rides.pipe';

describe('SortRidesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRidesPipe();
    expect(pipe).toBeTruthy();
  });
});
